import React from 'react';

function Sidebar({ onAppClick }) {
  return (
    <div
      style={{
        width: '70px',
        background: '#222',
        color: '#fff',
        height: '100vh',
        paddingTop: '20px',
        textAlign: 'center'
      }}
    >
      <div style={{ marginBottom: '20px' }} onClick={onAppClick}>📱</div>
      <div style={{ marginBottom: '20px' }}>💬</div>
      <div style={{ marginBottom: '20px' }}>⚙️</div>
    </div>
  );
}

export default Sidebar;
